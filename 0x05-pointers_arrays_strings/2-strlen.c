#imclude "main.h"

/**
 * int _strlen(char *s) - returning the length on an string
 * @s:string
 *return : length of a string
 */

int _strlen(char *s)
{
	int jay = 0;
	while (*(s + len) != '\0')
		jay++;

	return (len);
}
